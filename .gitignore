# Plaintext secrets — never commit. Back them up via ./secrets.sh, which writes
# the encrypted secrets.tar.gz.gpg (that blob, at the repo root, IS committed).
secrets/

# Generated / transient — these live in the GitHub Release, not in git.
# (publish.sh's scratch dir is an auto-removed mktemp, so it's not listed here.)
repo/
unsigned/
archive/
.fdroid.*
